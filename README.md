# ☀️ Clima Hoje (Weather App)

Este é um projeto de front-end para a interface de um aplicativo de previsão do tempo, chamado "Clima Hoje". O projeto foi desenvolvido como um exercício prático para aplicar conceitos fundamentais e avançados de HTML5 e CSS3, com foco total na construção de um layout complexo e responsivo sem o uso de frameworks.


---

### 🚀 Funcionalidades da Interface

Este projeto é uma demonstração de layout estático e não consome dados de uma API, focando exclusivamente nas habilidades de HTML e CSS.

* **Design Responsivo:** A aplicação se adapta perfeitamente a telas de desktop, tablets e celulares, utilizando Media Queries.
* **Barra de Busca:** Componente superior para pesquisa de localização.
* **Clima Atual (Showcase):** Seção de destaque com a informação principal do clima.
* **Previsão por Hora:** Um container com scroll horizontal (`overflow-x`) para visualização das próximas horas, feito com Flexbox.
* **Detalhes do Clima:** Um painel em grade (CSS Grid) exibindo detalhes como "Sensação Térmica", "Vento", "Chance de Chuva" e "Índice UV".
* **Previsão Semanal:** Uma lista da previsão dos próximos dias, utilizando Flexbox para garantir que os itens se distribuam igualmente.

---

### 🛠️ Tecnologias Utilizadas

O projeto foi construído do zero utilizando apenas as seguintes tecnologias:

* **HTML5:** Para a estrutura semântica do conteúdo.
* **CSS3:** Para toda a estilização, incluindo:
    * **Flexbox:** Usado extensivamente para alinhamento e distribuição dos componentes.
    * **CSS Grid Layout:** Utilizado para o painel de detalhes do clima.
    * **Media Queries:** Para aplicar os estilos responsivos do arquivo `mobile.css`.

---

### 📖 Aprendizados

Este projeto foi uma excelente oportunidade para praticar e solidificar os seguintes conceitos:

* Estruturação semântica de um projeto web.
* Separação de estilos (um CSS base e um `mobile.css` para responsividade).
* Uso prático de Flexbox para alinhamento e espaçamento de itens.
* Implementação de CSS Grid para layouts em colunas complexas.
* Criação de um design "desktop-first" (ou "mobile-first") e sua adaptação com Media Queries.
* Uso de `overflow` para criar áreas de rolagem dentro da página.

---

### Clone e Execute

Como este é um projeto estático, não há necessidade de instalação.

1.  Clone este repositório:
    ```bash
    git clone [https://github.com/VictorPrazeres/weather-app.git](https://github.com/VictorPrazeres/weather-app.git)
    ```
2.  Navegue até a pasta do projeto:
    ```bash
    cd weather-app
    ```
3.  Abra o arquivo `index.html` no seu navegador de preferência.
